# UI 구현 문제
1. var 키워드 사용시 포이스팅을 유발하고, 추후 벡엔드와 연동시 오류가 발생할 가능성이 있다. 이를 해결하기 위해 요즘은 let 키워드를 사용하는 추세다. 

2. const { birth, name, tomb, death } = king; 에서 tomb를 tombName으로 고쳐주거나 tomb = '어쩌고' 로 새로운 key - value propery 생성해주면 된다.

3. map() 안의 콜백 함수 중 인덱스 변경값을 return 해주지 않았기 때문.